 ======================== FASE 1 ========================
 
 # Lembrete

 Após acabar as alterações do projeto local

 -> COMMITT
 -> PUSH
 
só depois é que se passa para a próxima fase.


 ======================== FASE 1 ========================
# dentro da pasta do backend

- docker-compose down

- docker system prune --force --volumes --all

- docker-compose up -d


## para ser mais rápido
  docker-compose down && docker system prune --force --volumes --all && docker-compose up -d



# opcional
 docker rm -f $(docker ps -aq) or docker rm $(docker ps -a -f status=exited -q)
 docker rmi $(docker images -aq)



# Possiveis erros

" error: Your local changes to the following files would be overwritten by merge:
        backend/knexfile.js
Please commit your changes or stash them before you merge.
Aborting "

- git stash
- git pull

