 # dentro da pasta do backend
 
  - docker-compose down

  - docker system prune --force --volumes --all

  -docker-compose up

# opcional
 docker rm -f $(docker ps -aq) or docker rm $(docker ps -a -f status=exited -q)
 docker rmi $(docker images -aq)